---
// // CurrencyConverter.astro
// import fetch from 'node-fetch';

// const API_KEY = import.meta.env.EXCHANGE_RATE_API_KEY; // Replace with your actual API key
// const url = `https://v6.exchangerate-api.com/v6/${API_KEY}/pair/USD/COP`;

// // Function to fetch the current USD to COP exchange rate
// async function fetchExchangeRate() {
//     try {
//         const response = await fetch(url);
//         const data = await response.json();
        
//         if (data.result === 'success') {
//             return data.conversion_rate;
//         } else {
//             console.error("There was an error fetching the conversion rate.");
//             return null;
//         }
//     } catch (error) {
//         console.error("Error fetching conversion rate: ", error);
//         return null;
//     }
// }

// const { numMinimumWages = 1, amountInCOP = 0 } = Astro.props;
// const minimumWageCOP = 1300000; // Minimum wage in COP
// const conversionRate = await fetchExchangeRate();
// const wagesInUSD = conversionRate ? ((numMinimumWages * minimumWageCOP) / conversionRate).toFixed(2) : "N/A";
// const convertedAmount = conversionRate ? (amountInCOP / conversionRate).toFixed(2) : "N/A";
// ---

// <span>{amountInCOP} COP is approximately {convertedAmount} USD.</span>
