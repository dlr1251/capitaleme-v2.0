---
import { getCollection } from 'astro:content';
const benVisas = await getCollection('visas', ({ id, data }) => {
  return id.startsWith('en/') && data.beneficiaries.includes("no") === false;
});
---
<div>
    {
        benVisas.map( p => (            
            <ul>                  
                   
                <li><a href={`/visas/${p.slug.slice(3)}`}>{p.data.title.slice(3)}</a></li>
            </ul>
        ))
    }
</div>
